"use client"

/**
 * Thin alias over AppSidebar for future shell composition.
 * Pages should import DesktopSidebar when building adaptive layouts.
 */
export { AppSidebar as DesktopSidebar } from "./app-sidebar"
